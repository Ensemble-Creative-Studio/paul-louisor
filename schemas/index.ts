import blockContent from './blockContent'
import series from './series'
import page from './page'
import siteSettings from './siteSettings'

export const schemaTypes = [ blockContent, siteSettings, series, page]
